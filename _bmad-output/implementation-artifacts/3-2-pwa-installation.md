# Story 3.2: PWA Installation

Status: done

## Story

As a player,
I want to install ch3ss to my phone's home screen,
So that it feels like a native app and I can launch it with one tap.

## Acceptance Criteria

1. A Web App Manifest is served with: app name "ch3ss", icons (192x192, 512x512), theme color, standalone display mode (FR20)
2. The browser allows adding ch3ss to the home screen (Chrome install prompt, Safari Add to Home Screen)
3. Once installed, the app launches in standalone mode (no browser chrome — no URL bar, no browser tabs)
4. The app icon appears on the device home screen
5. The manifest theme_color and background_color match the dark (sable) theme

## Tasks / Subtasks

- [x] Task 1: Configure Web App Manifest in vite-plugin-pwa (AC: #1, #5)
  - [x] Added `manifest` config to existing VitePWA plugin in `vite.config.ts`
  - [x] Verified `theme_color` (#1a1a2e) matches the dark sable theme from `index.css`
  - [x] `background_color` = same as theme_color
- [x] Task 2: Create PWA icons (AC: #1, #4)
  - [x] Created `public/icons/icon-192.png` — 192x192px placeholder with chess motif
  - [x] Created `public/icons/icon-512.png` — 512x512px placeholder with chess motif
  - [x] 512px icon has `purpose: 'any maskable'` for Android adaptive icons
- [x] Task 3: Add theme-color meta tag to index.html (AC: #5)
  - [x] Added `<meta name="theme-color" content="#1a1a2e">` in `<head>`
  - [x] Matches manifest theme_color
- [x] Task 4: Add apple-touch-icon for Safari (AC: #2, #4)
  - [x] Added `<link rel="apple-touch-icon" href="/icons/icon-192.png">`
  - [x] Added `<meta name="apple-mobile-web-app-capable" content="yes">`
  - [x] Added `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">`
- [x] Task 5: Verify PWA installability (AC: #2, #3)
  - [x] Build succeeds with manifest.webmanifest generated in dist/
  - [x] Manifest contains correct name, icons, display mode, theme colors
- [x] Task 6: Write tests (AC: #1)
  - [x] Test: manifest config in vite.config.ts with correct name, icons, display mode
  - [x] Test: index.html contains theme-color meta tag
  - [x] Test: index.html contains apple-touch-icon link
  - [x] Test: index.html contains apple-mobile-web-app-capable
  - [x] Test: PWA icon files exist at public/icons/

## Dev Notes

### Architecture Compliance

- **Manifest generated by vite-plugin-pwa** — no manual `manifest.json` file needed
- **Icons in `public/icons/`** — matches architecture spec directory structure
- **Standalone display mode** — the app runs fullscreen without browser chrome

### Icon Requirements

Generated programmatic placeholder icons (chess-themed with knight silhouette, blue accent circle on dark background). These can be refined with proper design assets later.

### Theme Color Alignment

The `theme_color` (#1a1a2e) matches the CSS custom property `--color-bg-primary` from `src/index.css`.

### References

- [Source: _bmad-output/planning-artifacts/architecture.md#Project Structure — public/icons/]
- [Source: _bmad-output/planning-artifacts/epics.md#Story 3.2]
- [Source: _bmad-output/planning-artifacts/prd.md#FR20]

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
- No issues encountered

### Completion Notes List
- Added manifest config to VitePWA plugin with name, icons, display, theme colors
- Generated placeholder PWA icons (192x192, 512x512) programmatically
- Added theme-color and Apple-specific meta tags to index.html
- Build produces correct manifest.webmanifest with all required fields
- All 7 Story 3.2 tests pass, 97 total tests pass with 0 regressions

### File List
- vite.config.ts (modified — added manifest config to VitePWA)
- index.html (modified — added theme-color, apple-touch-icon, apple-mobile-web-app meta tags)
- public/icons/icon-192.png (created — 192x192 placeholder PWA icon)
- public/icons/icon-512.png (created — 512x512 placeholder PWA icon)
- src/pwa.test.ts (modified — added Story 3.2 tests)

## Change Log
- 2026-03-08: Configured PWA manifest, created placeholder icons, added Apple/theme meta tags
